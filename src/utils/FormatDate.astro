---
import { formatDistance } from 'date-fns'

interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-[24px] font-medium tracking-wide text-white">
	{
        formatDistance(date, new Date(), { addSuffix: true })
    }
</time>